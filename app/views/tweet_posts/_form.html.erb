<%= form_with(model: tweet_post) do |form| %>
  <% if tweet_post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tweet_post.errors.count, "error") %> prohibited this tweet_post from being saved:</h2>

      <ul>
        <% tweet_post.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :post %>
    <%= form.text_field :post %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, :value => current_user.id %>
  </div>

  <div class="field">
    <%= form.label :id_owner %>
    <%= form.text_field :id_owner, :value => current_user.id %>
    
  </div>


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
